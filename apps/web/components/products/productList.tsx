import type { Product } from "@repo/utils/tempData";

export function ProductList({ products }: { products: Product[] }) {
    return (
        <div>
            <p>Products Here!</p>
        </div>
    );
}